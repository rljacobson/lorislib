/*!

A version of Algorithm LM that adheres more strictly to the paper, that doesn't generate all matches or backtrack.

*/

mod solved_equation;






#[cfg(test)]
mod tests {
  #[test]
  fn it_works() {
    assert_eq!(2 + 2, 4);
  }
}
